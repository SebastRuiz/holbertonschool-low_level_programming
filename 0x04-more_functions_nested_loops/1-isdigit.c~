#include "holberton.h"
/**
 * _isdigit - Checks for a digit (0 through 9).
 * @c: The character to print.
 * Return: Returns 1 if is a digit, returns 0 otherwise.
 */
int _isdigit(int c)
{
	if (c > 47 && c < 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
